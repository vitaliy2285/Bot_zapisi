# YPlaces Telegram Mini App Clone

## Structure
- `backend/` FastAPI + SQLAlchemy + Alembic
- `frontend/` React 18 + Vite + Tailwind
- `nginx/` reverse proxy SSL config
- `docker-compose.yml` full stack orchestration

## Run
```bash
docker compose up --build
```
